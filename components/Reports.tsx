'use client';export default function Reports({reports,write}:{reports:any[];write:boolean}){async function create(type:string){const end=new Date().toISOString().slice(0,10),start=new Date(Date.now()-90*86400000).toISOString().slice(0,10);await fetch('/api/reports',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({type,title:`${type} Report · ${end}`,period_start:start,period_end:end})});location.reload()}return <div className="reportgrid"><section className="builder"><em>LIVE GOVERNANCE REPORTING</em><h2>Generate a decision-ready deck from the system of record.</h2><p>Current threat profile, risk register, remediation posture, incidents and governed documentation.</p><div className="slides">{[['01','Executive summary'],['02','Threat profile'],['03','Risks & agility'],['04','Incidents timeline']].map(x=><div key={x[0]}><small>{x[0]}</small><b>{x[1]}</b></div>)}</div>{write&&<footer><button className="primary light" onClick={()=>create('Board')}>Generate board deck</button><button className="ghost dark" onClick={()=>create('CyberGov')}>Generate CyberGov</button></footer>}</section><section><div className="sectiontitle"><em>GENERATED REPORTS</em><h2>Report library</h2></div><div className="reportlist">{reports.map(r=><article key={r.id}><div><span className="pill gray">{r.type}</span><h3>{r.title}</h3><p>{r.executive_summary}</p><small>{r.period_start} — {r.period_end}</small></div><a className="btn ghost" href={`/api/reports/${r.id}/download`}>Download .pptx</a></article>)}</div></section></div>}
