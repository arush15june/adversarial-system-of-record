import './globals.css';import Sidebar from '@/components/Sidebar';import {currentUser,users} from '@/lib/auth';export const metadata={title:'ACME Security System of Record',description:'Threat, Risk, Incident and Compliance demo'};export default async function Layout({children}:{children:React.ReactNode}){const u=await currentUser(),all=await users();return <html lang="en"><body><Sidebar user={u} users={all}/><main>{children}</main></body></html>}
