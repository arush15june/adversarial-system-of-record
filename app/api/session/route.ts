import {NextResponse} from 'next/server';import {currentUser,users} from '@/lib/auth';export async function GET(){return Response.json({user:await currentUser(),users:await users()})}export async function POST(r:Request){const b=await r.json();const all=await users();if(!all.some(x=>x.id===b.userId))return Response.json({error:'Unknown user'},{status:400});const res=NextResponse.json({ok:true});res.cookies.set('arm_user',b.userId,{httpOnly:true,sameSite:'lax',path:'/'});return res}
