# Local development creates a SQLite database under data/ automatically (Node 22+).
# For serverless production, configure a Turso/libSQL database:
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
# Optional override for local SQLite:
LOCAL_SQLITE_PATH=
